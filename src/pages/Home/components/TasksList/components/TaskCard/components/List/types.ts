export interface TaskPriorityValueTypes {
  priority: string
}

export interface TaskStatusValueTypes {
  status: string
}

export interface ListTypes {
  status: string,
  priority: string,
  dueDate?: string
}
